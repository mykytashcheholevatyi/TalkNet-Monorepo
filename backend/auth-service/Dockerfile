# Используйте официальный образ Python как базовый
FROM python:3.8-slim

# Установите рабочий каталог в контейнере
WORKDIR /app

# Копируйте файлы проекта в контейнер
COPY . /app

# Установите зависимости
RUN pip install --no-cache-dir -r requirements.txt

# Определите переменную окружения для включения режима отладки Flask (не используйте в продакшн!)
ENV FLASK_ENV=development

# Откройте порт, на котором работает Flask
EXPOSE 5000

# Определите команду для запуска приложения
CMD ["flask", "run", "--host=0.0.0.0"]
